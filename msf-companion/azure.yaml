# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: msf-companion
metadata:
  template: msf-companion
services:
  web:
    project: .
    language: ts
    host: containerapp
    docker:
      path: Dockerfile
      remoteBuild: true
